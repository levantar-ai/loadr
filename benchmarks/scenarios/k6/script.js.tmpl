import http from 'k6/http';

// Closed model: BENCH_VUS VUs for BENCH_DURATION hitting one GET.
// Rendered from this template by run.sh; CLI flags also pin --vus/--duration.
export const options = {
  vus: ${BENCH_VUS},
  duration: '${BENCH_DURATION}',
  summaryTrendStats: ['avg', 'min', 'med', 'p(90)', 'p(95)', 'p(99)', 'max'],
};

export default function () {
  http.get('${BENCH_URL}');
}

global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: loadr
    static_configs:
      - targets: ["controller:9091"]
        labels:
          service: loadr-controller

# Load the pre-declared topology (the `loadr.work` queue, user permissions) on
# boot so the loadr rabbitmq example and integration tests have a destination.
# Used by examples/harness/docker-compose.yml (rabbitmq:3.13-management).
load_definitions = /etc/rabbitmq/definitions.json

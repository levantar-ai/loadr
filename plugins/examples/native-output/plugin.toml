[plugin]
name = "file-report"
version = "0.1.0"
kind = "output"
type = "native"
entry = "libnative_output.so"
description = "Appends snapshot series counts and the final summary to a file"

[config]
path = "loadr-report.txt"

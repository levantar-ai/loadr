[plugin]
name = "echo-proto"
version = "0.1.0"
kind = "protocol"
type = "native"
entry = "libnative_protocol.so"
description = "Echo protocol: responds with the request body reversed"

[config]
prefix = ""

[plugin]
name = "upper-boundary"
version = "0.1.0"
kind = "extractor"
type = "wasm"
entry = "loadr_wasm_extractor.wasm"
description = "Boundary extractor that uppercases the matched value"

[config]
left = "<<"
right = ">>"

[plugin]
name = "jwt-decode"
version = "0.1.0"
kind = "extractor"
type = "wasm"
entry = "loadr_plugin_jwt_decode.wasm"
description = "Decodes a JWT and extracts the named claim from the payload"

[config]
claim = "sub"

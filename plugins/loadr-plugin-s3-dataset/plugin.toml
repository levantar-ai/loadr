[plugin]
name = "s3-dataset"
version = "1.0.0"
kind = "service"
type = "native"
entry = "libloadr_plugin_s3_dataset.so"
description = "S3 dataset feeder: fetches an object from S3 (SigV4) and parses CSV/JSONL into feeder rows"

# No required configuration here — the connection details (bucket/key/region and
# credentials) come from the feeder's per-use `config:` block in the test plan.
# Any keys set below would be merged as defaults under that block.
[config]
